- id: sensevoice-small
  kind: asr
  framework: onnx
  format: onnx
  default_device: auto
  compute_type: int8
  source:
    local_path: backend/models/pretrained/sensevoice
    repo_id: FunAudioLLM/SenseVoiceSmall
    files: [model_quant.onnx, tokens.json]
    mirrors: []
  resources: {vram_mb: 800, cpu_threads: 8}
  features: {languages: [zh, en, yue, ja, ko], timestamps: char}

- id: whisper-medium
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    local_path: models/huggingface/models--Systran--faster-whisper-medium/snapshots/08e178d48790749d25932bbc082711ddcfdfbc4f
    repo_id: Systran/faster-whisper-medium
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 2000}
  features: {timestamps: word, languages: multi}

# V3.2.0+dev.20260116.03: 添加所有 Whisper 模型支持
- id: whisper-tiny
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8
  source:
    repo_id: Systran/faster-whisper-tiny
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 500}
  features: {timestamps: word, languages: multi}

- id: whisper-base
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8
  source:
    repo_id: Systran/faster-whisper-base
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 800}
  features: {timestamps: word, languages: multi}

- id: whisper-small
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-whisper-small
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 1500}
  features: {timestamps: word, languages: multi}

- id: whisper-large
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-whisper-large
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 6000}
  features: {timestamps: word, languages: multi}

- id: whisper-large-v2
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-whisper-large-v2
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 6000}
  features: {timestamps: word, languages: multi}

- id: whisper-large-v3
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-whisper-large-v3
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 6000}
  features: {timestamps: word, languages: multi}

- id: whisper-turbo
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-whisper-large-v3-turbo
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 5000}
  features: {timestamps: word, languages: multi}

- id: whisper-distil-large-v3
  kind: asr
  framework: ctranslate2
  format: ct2
  default_device: cuda
  compute_type: int8_float16
  source:
    repo_id: Systran/faster-distil-whisper-large-v3
    files: [model.bin, config.json, tokenizer.json]
  resources: {vram_mb: 4000}
  features: {timestamps: word, languages: multi}

- id: brouhaha-quality
  kind: quality
  framework: torch
  format: pth
  default_device: auto
  compute_type: fp32
  source:
    local_path: backend/models/pretrained/brouhaha
    repo_id: pyannote/brouhaha
    files: [pytorch_model.bin, config.yaml]
  env:
    pip: ["pyannote.audio==3.1.1", "torch==2.1.2"]
  resources: {vram_mb: 1000}

- id: demucs-htdemucs
  kind: separation
  framework: demucs
  format: pth
  default_device: cuda
  compute_type: fp32
  source:
    local_path: backend/models/pretrained
    files: [htdemucs.th]
  features:
    model_name: htdemucs
  resources: {vram_mb: 1500}

- id: silero-vad
  kind: vad
  framework: onnx
  format: onnx
  default_device: auto
  compute_type: int8
  source:
    local_path: backend/models/pretrained/silero-vad
    files: [silero_vad.onnx]
  resources: {cpu_threads: 4}

# V3.2.0+dev.20260127.01: SpeechBrain LangID/声纹本地化
- id: langid-voxlingua
  kind: langid
  framework: torch
  format: pth
  default_device: cpu
  compute_type: fp32
  source:
    local_path: backend/models/pretrained/lang-id-voxlingua107-ecapa
    repo_id: speechbrain/lang-id-voxlingua107-ecapa
    files:
      [
        classifier.ckpt,
        embedding_model.ckpt,
        normalizer.ckpt,
        hyperparams.yaml,
        label_encoder.txt,
        config.json,
      ]
  resources: {cpu_threads: 4, vram_mb: 200}

- id: speaker-ecapa-voxceleb
  kind: speaker
  framework: torch
  format: pth
  default_device: cpu
  compute_type: fp32
  source:
    local_path: backend/models/pretrained/spkrec-ecapa-voxceleb
    repo_id: speechbrain/spkrec-ecapa-voxceleb
    files:
      [
        classifier.ckpt,
        embedding_model.ckpt,
        mean_var_norm_emb.ckpt,
        hyperparams.yaml,
        label_encoder.txt,
        config.json,
      ]
  resources: {cpu_threads: 4, vram_mb: 100}

- id: pyannote-segmentation-3-0
  kind: speaker
  framework: torch
  format: pth
  default_device: cuda
  compute_type: fp32
  source:
    local_path: backend/models/pretrained/segmentation-3.0
    files: [config.yaml, pytorch_model.bin]
  resources: {vram_mb: 1200, cpu_threads: 4}
  features:
    role: segmentation

- id: punct-ct-transformer-zh
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/ct_transformer_zh
    files: [model.onnx, config.json, tokens.json, vocab.txt]
  resources: {cpu_threads: 1}
  features: {languages: [zh, yue]}
  fallback: []


- id: punct-edge-punct-en
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/edge_punct_casing_en
    files: [model.onnx, model.int8.onnx, config.json, vocab.txt]
  resources: {cpu_threads: 1}
  features: {languages: [en]}
  fallback: []

- id: punct-distilbert-en
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/distilbert_punctuator_en
    files:
      [
        model_int8.onnx,
        config.json,
        tokenizer.json,
        tokenizer_config.json,
        special_tokens_map.json,
        vocab.txt,
      ]
  resources: {cpu_threads: 1}
  features: {languages: [en], model_file: model_int8.onnx}
  fallback: []

- id: punct-char-bert-ja
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/char_bert_ja
    files: [model.onnx, config.json, vocab.txt]
  resources: {cpu_threads: 1}
  features: {languages: [ja]}
  fallback: []

- id: punct-pcs-47lang
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/punct_cap_seg_47_language
    files:
      [
        punct_cap_seg_47lang_int8.onnx,
        spe_unigram_64k_lowercase_47lang.model,
        config.yaml,
      ]
  resources: {cpu_threads: 1}
  features: {languages: [ja]}
  fallback: [punct-char-bert-ja]
  fallback: [punct-distilbert-multi]

- id: punct-distilbert-multi
  kind: punct
  framework: onnx
  format: onnx
  default_device: cpu
  compute_type: int8
  source:
    local_path: backend/models/pretrained/punctuation/distilbert_multilang
    files: [model.onnx, config.json, vocab.txt]
  resources: {cpu_threads: 1}
  features: {languages: [auto, multi]}
  fallback: []
