# 转录层深度优化方案：时空解耦架构

> **文档版本**：v2.1
> **更新日期**：2025年12月4日
> **基于**：《新补充.md》时空解耦架构设计
> **影响范围**：Phase 1-3 的核心修改
> **目标**：建立 SenseVoice 为时间轴基准、Whisper 为内容补丁的双引擎架构

---

## ⚠️ 版本更新说明

### v2.1 新增（VAD优先 + 频谱分诊）

- ✅ **流程变更**：`音频提取 → VAD → 频谱分诊(Chunk级) → 按需分离 → 转录`
- ✅ **新增组件**：`AudioSpectrumClassifier` 频谱指纹分诊台
- ✅ **效率优化**：VAD先切分，只对需要分离的Chunk执行Demucs，节省时间和显存
- ✅ **OOM防护**：Chunk级处理避免超长音频一次性分离导致显存不足

> **详细设计**：参见 [Phase 2: 智能熔断集成](./02_Phase2_智能熔断集成_修订版.md) 第三节

---

## 一、架构概述

### 1.1 核心变更：时空解耦 (Time-Space Decoupling)

将音频处理管线中的"时间"与"内容"职责分离：

```
┌─────────────────────────────────────────────────────────────────┐
│                        时空解耦架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │  SenseVoice  │    │   Whisper    │    │     LLM      │      │
│  │  时间领主     │    │  听觉补丁    │    │   逻辑胶水    │      │
│  │  Timeline    │    │  Acoustic    │    │   Semantic   │      │
│  │   Truth      │    │   Patch      │    │   Refiner    │      │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘      │
│         │                   │                   │              │
│         ▼                   ▼                   ▼              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
│  │ 提供时间戳    │    │ 仅提供文本   │    │ 校对/仲裁    │      │
│  │ 基础文本      │    │ 弃用时间戳   │    │ 翻译         │      │
│  │ 字级置信度    │    │ [可选]对齐   │    │              │      │
│  └──────────────┘    └──────────────┘    └──────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 角色定义

| 角色 | 模型 | 职责 | 约束 |
|------|------|------|------|
| **时间领主** | SenseVoice | 提供绝对时间轴基准（Start/End）、基础文本、字级置信度 | 后续步骤**严禁修改**起止时间（除非启用强制对齐） |
| **听觉补丁** | Whisper | 针对低置信度片段二次"精听" | **默认仅提供文本**，使用伪对齐算法 |
| **逻辑胶水** | LLM | 错别字修正、歧义仲裁、翻译 | 最终文本裁决者 |

### 1.3 关键决策

1. **伪对齐算法**：Whisper/LLM 替换文本后，默认采用"线性时间平摊"策略
2. **前端虚拟滚动**：字级置信度数据量大，需实现 Virtual Scrolling
3. **流式输出**：各阶段完成即推送，实现实时字幕预览

---

## 二、前端预设方案体系

### 2.1 预设方案定义

前端界面提供 **6 个预设方案** + **高级自定义模式**：

| 预设 | 名称 | 组合 | 适用场景 |
|------|------|------|----------|
| **默认** | SenseVoice Only | SV | 极速预览、实时日志、测试 |
| **预设1** | 智能复核 | SV + Whisper Partial | 一般视频、日常Vlog |
| **预设2** | 轻度校对 | SV + Whisper Partial + LLM Partial Proof | 个人笔记、会议记录 |
| **预设3** | 深度校对 | SV + Whisper Partial + LLM Full Proof | 正式发布、文稿整理 |
| **预设4** | 校对+翻译（同步） | SV + Whisper Partial + LLM Full Proof + LLM Full Trans | 跨语言内容发布 |
| **预设5** | 校对+重点翻译 | SV + Whisper Partial + LLM Full Proof + LLM Partial Trans | 教学视频、重点标注 |

### 2.2 预设方案详细配置

```typescript
// frontend/src/constants/presets.ts

export interface PresetConfig {
  id: string;
  name: string;
  description: string;
  icon: string;
  // 转录层
  enhancement: 'off' | 'smart_patch' | 'deep_listen';
  // 校对层
  proofread: 'off' | 'sparse' | 'full';
  // 翻译层
  translate: 'off' | 'full' | 'partial';
  targetLanguage?: string;
  // 预估时间倍率（相对于视频时长）
  timeMultiplier: number;
}

export const PRESETS: PresetConfig[] = [
  {
    id: 'default',
    name: 'SenseVoice Only',
    description: '极速模式，仅使用 SenseVoice 转录',
    icon: '⚡',
    enhancement: 'off',
    proofread: 'off',
    translate: 'off',
    timeMultiplier: 0.1  // 10分钟视频约1分钟完成
  },
  {
    id: 'preset1',
    name: '智能复核',
    description: 'SV + Whisper 局部复核，平衡速度与质量',
    icon: '🎯',
    enhancement: 'smart_patch',
    proofread: 'off',
    translate: 'off',
    timeMultiplier: 0.15
  },
  {
    id: 'preset2',
    name: '轻度校对',
    description: '智能复核 + LLM 按需校对问题片段',
    icon: '✏️',
    enhancement: 'smart_patch',
    proofread: 'sparse',
    translate: 'off',
    timeMultiplier: 0.2
  },
  {
    id: 'preset3',
    name: '深度校对',
    description: '智能复核 + LLM 全文精修润色',
    icon: '📝',
    enhancement: 'smart_patch',
    proofread: 'full',
    translate: 'off',
    timeMultiplier: 0.3
  },
  {
    id: 'preset4',
    name: '校对+翻译',
    description: '深度校对 + 全文翻译（同步处理）',
    icon: '🌍',
    enhancement: 'smart_patch',
    proofread: 'full',
    translate: 'full',
    targetLanguage: 'en',
    timeMultiplier: 0.5
  },
  {
    id: 'preset5',
    name: '校对+重点翻译',
    description: '深度校对 + 仅翻译标记的重点段落',
    icon: '🎓',
    enhancement: 'smart_patch',
    proofread: 'full',
    translate: 'partial',
    timeMultiplier: 0.35
  }
];
```

### 2.3 高级自定义模式

用户可在"高级设置"中自由组合所有选项：

```typescript
// frontend/src/constants/advancedOptions.ts

export const ADVANCED_OPTIONS = {
  // ========== 转录层选项 ==========
  enhancement: {
    label: '增强模式',
    options: [
      { value: 'off', label: '关闭', desc: '仅 SenseVoice' },
      { value: 'smart_patch', label: '智能复核', desc: '低置信度自动 Whisper 复核' },
      { value: 'deep_listen', label: '深度重听', desc: 'Whisper 全文转录' }
    ]
  },

  // ========== 校对层选项 ==========
  proofread: {
    label: 'AI 校对',
    options: [
      { value: 'off', label: '关闭' },
      { value: 'sparse', label: '按需修复', desc: '仅校对低置信度和疑问片段' },
      { value: 'full', label: '全文精修', desc: '滑动窗口全量校对润色' }
    ]
  },

  // ========== 翻译层选项 ==========
  translate: {
    label: '翻译',
    options: [
      { value: 'off', label: '无' },
      { value: 'full', label: '全文翻译' },
      { value: 'partial', label: '部分翻译', desc: '仅翻译标记的重点段落' }
    ]
  },

  targetLanguage: {
    label: '目标语言',
    type: 'select',
    options: [
      { value: 'en', label: 'English' },
      { value: 'ja', label: '日本語' },
      { value: 'ko', label: '한국어' },
      { value: 'zh-TW', label: '繁體中文' }
    ],
    showWhen: (config) => config.translate !== 'off'
  }
};
```

---

## 三、Phase 修改清单

### 3.1 Phase 1 修改项

| 修改项 | 文件 | 类型 | 优先级 |
|--------|------|------|--------|
| 扩展 SentenceSegment 数据模型 | `sensevoice_models.py` | 修改 | P0 |
| 新增伪对齐算法 | `pseudo_alignment.py` | 新建 | P0 |
| 修改分句算法支持来源追踪 | `sentence_splitter.py` | 修改 | P1 |
| **新增智能进度系统** | `progress_tracker.py` | 新建 | P0 |
| **重构 SSE 服务** | `sse_service.py` | 修改 | P0 |

### 3.2 Phase 2 修改项

| 修改项 | 文件 | 类型 | 优先级 |
|--------|------|------|--------|
| 熔断决策增加"仅文本复核"选项 | `fuse_breaker.py` | 修改 | P0 |
| 新增组合方案配置 | `solution_matrix.py` | 新建 | P1 |
| **新增预设方案管理** | `preset_manager.py` | 新建 | P1 |

### 3.3 Phase 3 修改项

| 修改项 | 文件 | 类型 | 优先级 |
|--------|------|------|--------|
| Whisper 复核仅取文本+伪对齐 | `transcription_service.py` | 修改 | P0 |
| 支持组合方案矩阵 | `transcription_service.py` | 修改 | P1 |
| **字幕流式输出系统** | `streaming_subtitle.py` | 新建 | P0 |

### 3.4 Phase 4 修改项

| 修改项 | 文件 | 类型 | 优先级 |
|--------|------|------|--------|
| 前端虚拟滚动 | `TaskListView.vue` | 修改 | P1 |
| **预设选择器组件** | `PresetSelector.vue` | 新增 | P0 |
| **高级设置面板** | `AdvancedSettings.vue` | 新增 | P1 |
| **置信度警告高亮系统** | `ConfidenceHighlight.vue` | 新增 | P0 |

### 3.5 基础数据模型修改

以下是对现有 `job_models.py` 和 API 模型的修改说明：

#### 3.5.1 JobSettings 扩展

**文件**: `backend/app/models/job_models.py`

```python
@dataclass
class TranscriptionPreset:
    """转录预设配置"""
    preset_id: str = "default"                     # 预设ID: default/preset1-5/custom
    enhancement: str = "off"                       # 增强模式: off/smart_patch/deep_listen
    proofread: str = "off"                         # 校对: off/sparse/full
    translate: str = "off"                         # 翻译: off/full/partial
    target_language: Optional[str] = None          # 翻译目标语言


@dataclass
class JobSettings:
    """转录任务设置（扩展版）"""
    model: str = "medium"
    compute_type: str = "float16"
    device: str = "cuda" if torch.cuda.is_available() else "cpu"
    batch_size: int = 16
    word_timestamps: bool = False
    cpu_affinity: Optional["CPUAffinityConfig"] = None
    demucs: DemucsSettings = field(default_factory=DemucsSettings)
    # ========== 新增：预设配置 ==========
    preset: TranscriptionPreset = field(default_factory=TranscriptionPreset)
```

#### 3.5.2 DemucsSettings 扩展

```python
@dataclass
class DemucsSettings:
    """Demucs人声分离配置"""
    # === 原有字段 ===
    enabled: bool = True
    mode: str = "auto"
    # ... 其他原有字段 ...
```

#### 3.5.3 JobState 扩展

```python
@dataclass
class SegmentProgress:
    """单个片段的处理进度（用于流式输出）"""
    segment_index: int
    total_segments: int
    phase: str                  # sv_transcribe/whisper_patch/llm_proof/llm_trans
    status: str                 # pending/processing/completed/failed
    text: Optional[str] = None
    confidence: Optional[float] = None


@dataclass
class JobState:
    """转录任务状态（扩展版）"""
    # === 原有字段 ===
    job_id: str
    filename: str
    # ... 其他原有字段 ...
    
    # ========== 新增：流式输出支持 ==========
    streaming_enabled: bool = True               # 是否启用流式输出
    current_segment_index: int = 0               # 当前处理的片段索引
    segments_sv_completed: int = 0               # SV 转录完成数
    segments_whisper_patched: int = 0            # Whisper 复核完成数
    segments_llm_proofread: int = 0              # LLM 校对完成数
    segments_llm_translated: int = 0             # LLM 翻译完成数
    
    # ========== 新增：预设追踪 ==========
    preset_id: str = "default"                   # 使用的预设ID
    
    def to_dict(self):
        """转换为字典格式，用于API响应（扩展版）"""
        d = asdict(self)
        d.pop('segments', None)
        # 新增流式进度信息
        d['streaming_progress'] = {
            'sv_completed': self.segments_sv_completed,
            'whisper_patched': self.segments_whisper_patched,
            'llm_proofread': self.segments_llm_proofread,
            'llm_translated': self.segments_llm_translated
        }
        return d
```

#### 3.5.4 API 请求模型扩展

**文件**: `backend/app/api/routes/transcription_routes.py`

```python
class TranscriptionPresetAPI(BaseModel):
    """预设配置请求模型"""
    preset_id: str = "default"
    enhancement: str = "off"
    proofread: str = "off"
    translate: str = "off"
    target_language: Optional[str] = None


class DemucsSettingsAPI(BaseModel):
    """Demucs配置请求模型"""
    enabled: bool = True
    mode: str = "auto"
    retry_threshold_logprob: float = -0.8
    retry_threshold_no_speech: float = 0.6
    circuit_breaker_enabled: bool = True
    consecutive_threshold: int = 3
    ratio_threshold: float = 0.2


class TranscribeSettings(BaseModel):
    """转录设置请求模型（扩展版）"""
    model: str = "medium"
    compute_type: str = "float16"
    device: str = "cuda"
    batch_size: int = 16
    word_timestamps: bool = False
    demucs: Optional[DemucsSettingsAPI] = None
    # ========== 新增 ==========
    preset: Optional[TranscriptionPresetAPI] = None
```

#### 3.5.5 持久化兼容性

修改 `to_meta_dict()` 和 `from_meta_dict()` 方法以支持新字段：

```python
def to_meta_dict(self) -> dict:
    """扩展版元信息字典"""
    base = {
        # ... 原有字段 ...
        "settings": {
            # ... 原有设置字段 ...
            "preset": {
                "preset_id": self.settings.preset.preset_id,
                "enhancement": self.settings.preset.enhancement,
                "proofread": self.settings.preset.proofread,
                "translate": self.settings.preset.translate,
                "target_language": self.settings.preset.target_language
            }
        },
        # 新增流式进度
        "streaming_progress": {
            "sv_completed": self.segments_sv_completed,
            "whisper_patched": self.segments_whisper_patched,
            "llm_proofread": self.segments_llm_proofread,
            "llm_translated": self.segments_llm_translated
        },
        "preset_id": self.preset_id
    }
    return base


@classmethod
def from_meta_dict(cls, data: dict) -> "JobState":
    """扩展版恢复方法（向后兼容）"""
    # 处理预设配置（向后兼容：如果没有则使用默认值）
    preset_data = data.get("settings", {}).get("preset", {})
    preset = TranscriptionPreset(
        preset_id=preset_data.get("preset_id", "default"),
        enhancement=preset_data.get("enhancement", "off"),
        proofread=preset_data.get("proofread", "off"),
        translate=preset_data.get("translate", "off"),
        target_language=preset_data.get("target_language")
    )
    
    # ... 创建 JobState 实例 ...
    
    # 恢复流式进度（向后兼容）
    streaming_progress = data.get("streaming_progress", {})
    job.segments_sv_completed = streaming_progress.get("sv_completed", 0)
    job.segments_whisper_patched = streaming_progress.get("whisper_patched", 0)
    job.segments_llm_proofread = streaming_progress.get("llm_proofread", 0)
    job.segments_llm_translated = streaming_progress.get("llm_translated", 0)
    job.preset_id = data.get("preset_id", "default")
    
    return job
```

#### 3.5.6 model_models.py 扩展

**文件**: `backend/app/models/model_models.py`

```python
@dataclass
class SenseVoiceModelInfo:
    """SenseVoice模型信息（新增）"""
    model_id: str = "iic/SenseVoiceSmall"
    status: str = "not_downloaded"      # not_downloaded/downloading/ready/error
    download_progress: float = 0.0
    local_path: Optional[str] = None
    
    def to_dict(self):
        return {
            "model_id": self.model_id,
            "status": self.status,
            "download_progress": self.download_progress,
            "local_path": str(self.local_path) if self.local_path else None
        }


```

---

## 四、核心修改详情

### 4.1 数据模型扩展（Phase 1）

**文件**: `backend/app/models/sensevoice_models.py`

```python
from dataclasses import dataclass, field
from typing import List, Optional
from enum import Enum


class TextSource(Enum):
    """文本来源"""
    SENSEVOICE = "sensevoice"        # SenseVoice 原始输出
    WHISPER_PATCH = "whisper_patch"  # Whisper 复核替换
    LLM_CORRECTION = "llm_correction"  # LLM 校对修正
    LLM_TRANSLATION = "llm_translation"  # LLM 翻译


class WarningType(Enum):
    """警告类型（用于高亮系统）"""
    NONE = "none"                           # 无警告
    LOW_TRANSCRIPTION_CONFIDENCE = "low_transcription"  # 转录置信度低
    HIGH_PROOFREAD_PERPLEXITY = "high_perplexity"      # 校对困惑度高
    BOTH = "both"                           # 两者都有问题


@dataclass
class WordTimestamp:
    """字级时间戳"""
    word: str
    start: float
    end: float
    confidence: float = 1.0
    is_pseudo: bool = False  # 是否为伪对齐生成
    # ========== 新增：警告字段 ==========
    warning_type: WarningType = WarningType.NONE
    perplexity: Optional[float] = None  # LLM 校对时的困惑度


@dataclass
class SentenceSegment:
    """句级字幕段（扩展版）"""
    text: str
    start: float
    end: float
    words: List[WordTimestamp]
    confidence: float = 1.0

    # ========== 时空解耦字段 ==========
    source: TextSource = TextSource.SENSEVOICE  # 文本来源
    is_modified: bool = False                    # 是否被修改过
    original_text: Optional[str] = None          # 修改前的原始文本
    whisper_alternative: Optional[str] = None    # Whisper 备选文本

    # ========== 新增：警告与校对字段 ==========
    warning_type: WarningType = WarningType.NONE  # 句级警告类型
    perplexity: Optional[float] = None            # LLM 校对困惑度
    translation: Optional[str] = None             # 翻译结果
    translation_confidence: Optional[float] = None  # 翻译置信度

    def mark_as_modified(self, new_text: str, source: TextSource):
        """标记为已修改"""
        if not self.is_modified:
            self.original_text = self.text
        self.text = new_text
        self.source = source
        self.is_modified = True

    def compute_warning_type(self) -> WarningType:
        """根据置信度和困惑度计算警告类型"""
        has_low_confidence = self.confidence < 0.6
        has_high_perplexity = self.perplexity is not None and self.perplexity > 50.0

        if has_low_confidence and has_high_perplexity:
            return WarningType.BOTH
        elif has_low_confidence:
            return WarningType.LOW_TRANSCRIPTION_CONFIDENCE
        elif has_high_perplexity:
            return WarningType.HIGH_PROOFREAD_PERPLEXITY
        return WarningType.NONE
```

### 4.2 伪对齐算法（Phase 1 新增）

**文件**: `backend/app/services/pseudo_alignment.py`

```python
"""
伪对齐算法（Pseudo-Alignment）

核心原理：
- SenseVoice 确定的时间窗口（start/end）不可变
- 当 Whisper/LLM 替换文本后，新字符均匀分布在原时间窗口内
- 生成的字级时间戳标记为 is_pseudo=True
"""
from typing import List
from ..models.sensevoice_models import WordTimestamp
import logging

logger = logging.getLogger(__name__)


class PseudoAlignment:
    """伪对齐器"""

    @staticmethod
    def apply(
        original_start: float,
        original_end: float,
        new_text: str,
        default_confidence: float = 1.0
    ) -> List[WordTimestamp]:
        """
        将新文本均匀映射到原时间段内

        Args:
            original_start: 原始起始时间（由 SenseVoice 确定，不可变）
            original_end: 原始结束时间（由 SenseVoice 确定，不可变）
            new_text: 替换后的新文本
            default_confidence: 默认置信度（修正后通常为 1.0）

        Returns:
            List[WordTimestamp]: 伪对齐的字级时间戳列表
        """
        duration = original_end - original_start

        # 过滤空白字符，保留实际字符
        chars = [c for c in new_text if not c.isspace()]
        char_count = len(chars)

        if char_count == 0:
            logger.warning(f"伪对齐：文本为空，跳过")
            return []

        # 计算每个字符的时长
        step = duration / char_count
        result = []

        for i, char in enumerate(chars):
            w_start = original_start + (i * step)
            w_end = w_start + step

            result.append(WordTimestamp(
                word=char,
                start=round(w_start, 3),
                end=round(w_end, 3),
                confidence=default_confidence,
                is_pseudo=True  # 标记为伪对齐生成
            ))

        logger.debug(
            f"伪对齐完成: {char_count} 字符, "
            f"时间窗口 {original_start:.2f}-{original_end:.2f}s"
        )

        return result

    @staticmethod
    def apply_to_sentence(
        sentence,  # SentenceSegment
        new_text: str,
        source  # TextSource
    ):
        """
        对句子应用伪对齐

        Args:
            sentence: 原始句子段落
            new_text: 替换后的文本
            source: 文本来源

        Returns:
            修改后的 SentenceSegment（原对象被修改）
        """
        # 生成新的字级时间戳
        new_words = PseudoAlignment.apply(
            original_start=sentence.start,
            original_end=sentence.end,
            new_text=new_text
        )

        # 更新句子
        sentence.mark_as_modified(new_text, source)
        sentence.words = new_words

        return sentence


# ========== 单例访问 ==========

_pseudo_alignment_instance = None


def get_pseudo_alignment() -> PseudoAlignment:
    """获取伪对齐器单例"""
    global _pseudo_alignment_instance
    if _pseudo_alignment_instance is None:
        _pseudo_alignment_instance = PseudoAlignment()
    return _pseudo_alignment_instance
```


### 4.4 熔断决策扩展（Phase 2 修改）

**文件**: `backend/app/services/fuse_breaker.py`

在现有 `FuseAction` 枚举中添加新动作：

```python
class FuseAction(Enum):
    """熔断动作（扩展）"""
    ACCEPT = "accept"                           # 接受结果
    UPGRADE_SEPARATION = "upgrade_separation"   # 升级分离模型
    WHISPER_RETRY = "whisper_retry"             # Whisper 复核（全量重试）

    # ========== 新增：时空解耦动作 ==========
    WHISPER_TEXT_ONLY = "whisper_text_only"     # Whisper 仅取文本（伪对齐）
    LLM_ARBITRATE = "llm_arbitrate"             # LLM 仲裁（SV vs Whisper）
```

修改 `decide_action` 方法，支持新的决策逻辑：

```python
def decide_action(
    self,
    segment_id: int,
    confidence: float,
    event_tag: Optional[str],
    current_separation_level: SeparationLevel,
    whisper_alternative: Optional[str] = None
) -> FuseDecision:
    """
    决策熔断动作（扩展版）

    决策优先级：
    1. 高置信度 → ACCEPT
    2. 有噪音标签 + 未升级 → UPGRADE_SEPARATION
    3. 低置信度 + 已升级 → WHISPER_TEXT_ONLY（新）
    4. 有 Whisper 备选 + 歧义 → LLM_ARBITRATE（新）
    """
    state = self._get_or_create_state(segment_id, current_separation_level)

    # 1. 高置信度，直接接受
    if confidence >= self.confidence_threshold:
        return FuseDecision(
            action=FuseAction.ACCEPT,
            reason=f"置信度 {confidence:.2f} >= {self.confidence_threshold}"
        )

    # 2. 检查止损点
    if state.retry_count >= self.max_retry_count:
        return FuseDecision(
            action=FuseAction.ACCEPT,
            reason=f"达到止损点 (重试 {state.retry_count} 次)"
        )

    # 3. 噪音环境 + 可升级 → 升级分离
    if event_tag in ['BGM', 'Noise', 'Music']:
        next_level = self._get_next_separation_level(current_separation_level)
        if next_level:
            state.retry_count += 1
            return FuseDecision(
                action=FuseAction.UPGRADE_SEPARATION,
                reason=f"检测到 {event_tag}，升级分离模型",
                next_separation_level=next_level
            )

    # 4. 低置信度 + 已完全升级 → Whisper 仅取文本（伪对齐）
    if confidence < self.upgrade_threshold:
        state.retry_count += 1

        return FuseDecision(
            action=FuseAction.WHISPER_TEXT_ONLY,
            reason=f"低置信度 {confidence:.2f}，使用 Whisper 复核（伪对齐）"
        )

    # 5. 有 Whisper 备选且存在歧义 → LLM 仲裁
    if whisper_alternative:
        return FuseDecision(
            action=FuseAction.LLM_ARBITRATE,
            reason="SenseVoice 与 Whisper 结果存在差异，需 LLM 仲裁"
        )

    # 默认：接受
    return FuseDecision(
        action=FuseAction.ACCEPT,
        reason="默认接受"
    )
```

### 4.5 Whisper 复核重构（Phase 3 核心修改）

**文件**: `backend/app/services/transcription_service.py`

```python
async def _whisper_text_only_patch(
    self,
    sentence: SentenceSegment,
    audio_array: np.ndarray,
    previous_text: str = ""
) -> SentenceSegment:
    """
    Whisper 仅文本复核（时空解耦核心）

    关键原则：
    - 仅使用 Whisper 的文本输出
    - 使用伪对齐算法生成字级时间戳

    Args:
        sentence: 原始句子（SenseVoice 输出）
        audio_array: 音频数组
        previous_text: 前一句文本（用于 initial_prompt）

    Returns:
        修正后的句子（时间戳保持不变）
    """
    from .whisper_service import get_whisper_service
    from .pseudo_alignment import get_pseudo_alignment
    from .text_normalizer import TextNormalizer

    # 1. 提取音频片段
    audio_segment = self._extract_segment_audio(sentence, audio_array)

    # 2. Whisper 转录（仅取文本）
    whisper_service = get_whisper_service()
    result = whisper_service.transcribe(
        audio=audio_segment,
        initial_prompt=previous_text,
        language=self.current_job.settings.language
    )

    # 3. 获取 Whisper 文本（弃用时间戳）
    whisper_text = result.get('text', '').strip()

    if not whisper_text:
        self.logger.warning(f"Whisper 复核返回空文本，保留原结果")
        return sentence

    # 4. 文本清洗
    whisper_text = TextNormalizer.process(whisper_text)

    # 5. 应用伪对齐
    pseudo_alignment = get_pseudo_alignment()
    pseudo_alignment.apply_to_sentence(
        sentence=sentence,
        new_text=whisper_text,
        source=TextSource.WHISPER_PATCH
    )

    self.logger.info(
        f"Whisper 复核完成: '{sentence.original_text}' -> '{sentence.text}'"
    )

    return sentence
```

---

## 五、智能进度系统

### 5.1 进度权重配置

**文件**: `backend/app/services/progress_tracker.py`

根据用户选择的预设/自定义配置，动态调整各阶段的进度权重：

```python
"""
智能进度追踪系统

核心特性：
1. 根据预设配置动态调整各阶段权重
2. 支持流式更新（SV、Whisper、LLM 各阶段独立更新）
3. 统一 SSE 事件 tag 设计
"""
from dataclasses import dataclass, field
from typing import Dict, Optional, List
from enum import Enum
import logging

logger = logging.getLogger(__name__)


class ProcessPhase(Enum):
    """处理阶段枚举"""
    PENDING = "pending"           # 等待开始
    EXTRACT = "extract"           # 音频提取
    BGM_DETECT = "bgm_detect"     # BGM 检测
    DEMUCS = "demucs"             # 人声分离
    VAD = "vad"                   # VAD 分段
    SENSEVOICE = "sensevoice"     # SenseVoice 转录
    WHISPER_PATCH = "whisper"     # Whisper 复核
    LLM_PROOF = "llm_proof"       # LLM 校对
    LLM_TRANS = "llm_trans"       # LLM 翻译
    SRT = "srt"                   # SRT 生成
    COMPLETE = "complete"         # 完成


@dataclass
class PhaseProgress:
    """阶段进度"""
    phase: ProcessPhase
    weight: float              # 该阶段在总进度中的权重
    total_items: int = 0       # 总项目数（如总段数）
    completed_items: int = 0   # 已完成项目数
    is_active: bool = False    # 是否正在处理
    message: str = ""          # 进度消息


@dataclass
class PresetWeights:
    """预设权重配置"""
    extract: float = 5
    bgm_detect: float = 2
    demucs: float = 8
    vad: float = 5
    sensevoice: float = 40
    whisper: float = 0        # 默认关闭
    llm_proof: float = 0      # 默认关闭
    llm_trans: float = 0      # 默认关闭
    srt: float = 10

    @classmethod
    def from_preset(cls, preset_id: str) -> 'PresetWeights':
        """根据预设ID生成权重配置"""
        presets = {
            'default': cls(
                # SenseVoice Only
                sensevoice=50, srt=10
            ),
            'preset1': cls(
                # SV + Whisper Partial
                sensevoice=35, whisper=20, srt=10
            ),
            'preset2': cls(
                # SV + Whisper Partial + LLM Partial Proof
                sensevoice=30, whisper=15, llm_proof=15, srt=10
            ),
            'preset3': cls(
                # SV + Whisper Partial + LLM Full Proof
                sensevoice=25, whisper=15, llm_proof=25, srt=10
            ),
            'preset4': cls(
                # SV + Whisper Partial + LLM Full Proof + LLM Full Trans
                sensevoice=20, whisper=10, llm_proof=20, llm_trans=15, srt=10
            ),
            'preset5': cls(
                # SV + Whisper Partial + LLM Full Proof + LLM Partial Trans
                sensevoice=22, whisper=12, llm_proof=20, llm_trans=8, srt=10
            ),
        }
        return presets.get(preset_id, cls())

    def total_weight(self) -> float:
        """计算总权重"""
        return (
            self.extract + self.bgm_detect + self.demucs +
            self.vad + self.sensevoice + self.whisper +
            self.llm_proof + self.llm_trans + self.srt
        )


class ProgressTracker:
    """智能进度追踪器"""

    def __init__(self, job_id: str, preset_id: str = 'default'):
        self.job_id = job_id
        self.weights = PresetWeights.from_preset(preset_id)
        self.total_weight = self.weights.total_weight()

        # 各阶段进度状态
        self.phases: Dict[ProcessPhase, PhaseProgress] = {}
        self._init_phases()

        # 累计完成权重
        self.completed_weight = 0.0
        self.current_phase = ProcessPhase.PENDING

        logger.info(f"进度追踪器初始化: preset={preset_id}, total_weight={self.total_weight}")

    def _init_phases(self):
        """初始化各阶段"""
        weight_map = {
            ProcessPhase.EXTRACT: self.weights.extract,
            ProcessPhase.BGM_DETECT: self.weights.bgm_detect,
            ProcessPhase.DEMUCS: self.weights.demucs,
            ProcessPhase.VAD: self.weights.vad,
            ProcessPhase.SENSEVOICE: self.weights.sensevoice,
            ProcessPhase.WHISPER_PATCH: self.weights.whisper,
            ProcessPhase.LLM_PROOF: self.weights.llm_proof,
            ProcessPhase.LLM_TRANS: self.weights.llm_trans,
            ProcessPhase.SRT: self.weights.srt,
        }

        for phase, weight in weight_map.items():
            self.phases[phase] = PhaseProgress(
                phase=phase,
                weight=weight,
                is_active=False
            )

    def start_phase(self, phase: ProcessPhase, total_items: int = 1, message: str = ""):
        """开始某个阶段"""
        if phase in self.phases:
            self.phases[phase].is_active = True
            self.phases[phase].total_items = total_items
            self.phases[phase].completed_items = 0
            self.phases[phase].message = message
            self.current_phase = phase
            logger.debug(f"阶段开始: {phase.value}, 总项目: {total_items}")

    def update_phase(
        self,
        phase: ProcessPhase,
        completed: int = None,
        increment: int = None,
        message: str = None
    ):
        """更新阶段进度"""
        if phase not in self.phases:
            return

        pp = self.phases[phase]

        if completed is not None:
            pp.completed_items = completed
        elif increment is not None:
            pp.completed_items += increment

        if message is not None:
            pp.message = message

    def complete_phase(self, phase: ProcessPhase):
        """完成某个阶段"""
        if phase in self.phases:
            pp = self.phases[phase]
            pp.is_active = False
            pp.completed_items = pp.total_items
            self.completed_weight += pp.weight
            logger.debug(f"阶段完成: {phase.value}")

    def get_overall_progress(self) -> float:
        """获取总体进度百分比"""
        # 已完成阶段的权重
        progress = self.completed_weight

        # 加上当前阶段的部分进度
        if self.current_phase in self.phases:
            pp = self.phases[self.current_phase]
            if pp.is_active and pp.total_items > 0:
                phase_progress = pp.completed_items / pp.total_items
                progress += pp.weight * phase_progress

        return round((progress / self.total_weight) * 100, 1)

    def get_phase_progress(self, phase: ProcessPhase) -> float:
        """获取某阶段的进度百分比"""
        if phase not in self.phases:
            return 0.0

        pp = self.phases[phase]
        if pp.total_items == 0:
            return 0.0

        return round((pp.completed_items / pp.total_items) * 100, 1)

    def to_sse_data(self) -> dict:
        """生成 SSE 推送数据"""
        return {
            "job_id": self.job_id,
            "phase": self.current_phase.value,
            "percent": self.get_overall_progress(),
            "phase_percent": self.get_phase_progress(self.current_phase),
            "message": self.phases.get(self.current_phase, PhaseProgress(
                phase=self.current_phase, weight=0
            )).message,
            "phases": {
                p.value: {
                    "weight": self.phases[p].weight,
                    "progress": self.get_phase_progress(p),
                    "completed": self.phases[p].completed_items,
                    "total": self.phases[p].total_items,
                    "active": self.phases[p].is_active
                }
                for p in self.phases
            }
        }


# ========== 单例工厂 ==========

_tracker_instances: Dict[str, ProgressTracker] = {}


def get_progress_tracker(job_id: str, preset_id: str = None) -> ProgressTracker:
    """获取或创建进度追踪器"""
    global _tracker_instances

    if job_id not in _tracker_instances:
        if preset_id is None:
            preset_id = 'default'
        _tracker_instances[job_id] = ProgressTracker(job_id, preset_id)

    return _tracker_instances[job_id]


def remove_progress_tracker(job_id: str):
    """移除进度追踪器"""
    global _tracker_instances
    if job_id in _tracker_instances:
        del _tracker_instances[job_id]
```

### 5.2 SSE 事件 Tag 设计

**文件**: `backend/app/services/sse_service.py` (扩展)

```python
"""
SSE 事件 Tag 统一定义

事件命名规范：
- progress.{phase}: 进度更新事件
- subtitle.{action}: 字幕流式事件
- signal.{type}: 信号事件
"""

# ========== 进度事件 Tag ==========

SSE_PROGRESS_TAGS = {
    # 整体进度
    "progress.overall": "总体进度更新",

    # 各阶段进度
    "progress.extract": "音频提取进度",
    "progress.bgm_detect": "BGM 检测进度",
    "progress.demucs": "人声分离进度",
    "progress.vad": "VAD 分段进度",
    "progress.sensevoice": "SenseVoice 转录进度（流式）",
    "progress.whisper": "Whisper 复核进度",
    "progress.llm_proof": "LLM 校对进度",
    "progress.llm_trans": "LLM 翻译进度",
    "progress.srt": "SRT 生成进度",
}

# ========== 字幕流式事件 Tag ==========

SSE_SUBTITLE_TAGS = {
    # SenseVoice 流式输出
    "subtitle.sv_segment": "SenseVoice 完成一个 VAD 段",
    "subtitle.sv_sentence": "SenseVoice 完成一个句子",

    # Whisper 覆盖
    "subtitle.whisper_patch": "Whisper 复核覆盖一个句子",

    # LLM 覆盖
    "subtitle.llm_proof": "LLM 校对覆盖一个句子",
    "subtitle.llm_trans": "LLM 翻译完成一个句子",

    # 批量更新
    "subtitle.batch_update": "批量更新多个句子",
}

# ========== 信号事件 Tag ==========

SSE_SIGNAL_TAGS = {
    "signal.job_start": "任务开始",
    "signal.job_complete": "任务完成",
    "signal.job_failed": "任务失败",
    "signal.job_paused": "任务暂停",
    "signal.job_canceled": "任务取消",
    "signal.phase_start": "阶段开始",
    "signal.phase_complete": "阶段完成",
    "signal.circuit_breaker": "熔断触发",
    "signal.model_escalation": "模型升级",
}


# ========== SSE 推送辅助方法 ==========

def push_progress_event(
    sse_manager,
    job_id: str,
    phase: str,
    data: dict
):
    """推送进度事件"""
    tag = f"progress.{phase}"
    channel_id = f"job:{job_id}"
    sse_manager.broadcast_sync(channel_id, tag, data)

    # 同时推送到全局频道
    sse_manager.broadcast_sync("global", "job_progress", {
        "id": job_id,
        **data
    })


def push_subtitle_event(
    sse_manager,
    job_id: str,
    event_type: str,
    sentence_data: dict
):
    """推送字幕流式事件"""
    tag = f"subtitle.{event_type}"
    channel_id = f"job:{job_id}"
    sse_manager.broadcast_sync(channel_id, tag, sentence_data)


def push_signal_event(
    sse_manager,
    job_id: str,
    signal_type: str,
    message: str = ""
):
    """推送信号事件"""
    tag = f"signal.{signal_type}"
    channel_id = f"job:{job_id}"
    sse_manager.broadcast_sync(channel_id, tag, {
        "job_id": job_id,
        "signal": signal_type,
        "message": message
    })
```

---

## 六、组合方案矩阵

### 6.1 方案配置（Phase 2 新增）

**文件**: `backend/app/services/solution_matrix.py`

```python
"""
组合方案矩阵 (Solution Matrix)

提供高度模块化的配置，允许在"速度、成本、质量"之间自由组合。
前端预设对应后端具体配置。
"""
from dataclasses import dataclass
from enum import Enum
from typing import Optional


class EnhancementMode(Enum):
    """增强模式"""
    OFF = "off"                      # A1: SenseVoice Only
    SMART_PATCH = "smart_patch"     # B1: SenseVoice + Whisper Partial（推荐）
    DEEP_LISTEN = "deep_listen"     # C1: SenseVoice + Whisper Full


class ProofreadMode(Enum):
    """校对模式"""
    OFF = "off"                   # 关闭
    SPARSE = "sparse"             # P1: 按需修复（推荐）
    FULL = "full"                 # P2: 全文精修


class TranslateMode(Enum):
    """翻译模式"""
    OFF = "off"                   # 无翻译
    FULL = "full"                 # T1: 全量翻译
    PARTIAL = "partial"           # T2: 部分翻译


@dataclass
class SolutionConfig:
    """方案配置"""
    preset_id: str = "default"                    # 预设ID
    enhancement: EnhancementMode = EnhancementMode.OFF
    proofread: ProofreadMode = ProofreadMode.OFF
    translate: TranslateMode = TranslateMode.OFF
    target_language: Optional[str] = None

    # 高级选项
    confidence_threshold: float = 0.6             # 低于此值触发复核

    @classmethod
    def from_preset(cls, preset_id: str) -> 'SolutionConfig':
        """根据预设ID创建配置"""
        presets = {
            'default': cls(
                preset_id='default',
                enhancement=EnhancementMode.OFF,
                proofread=ProofreadMode.OFF,
                translate=TranslateMode.OFF
            ),
            'preset1': cls(
                preset_id='preset1',
                enhancement=EnhancementMode.SMART_PATCH,
                proofread=ProofreadMode.OFF,
                translate=TranslateMode.OFF
            ),
            'preset2': cls(
                preset_id='preset2',
                enhancement=EnhancementMode.SMART_PATCH,
                proofread=ProofreadMode.SPARSE,
                translate=TranslateMode.OFF
            ),
            'preset3': cls(
                preset_id='preset3',
                enhancement=EnhancementMode.SMART_PATCH,
                proofread=ProofreadMode.FULL,
                translate=TranslateMode.OFF
            ),
            'preset4': cls(
                preset_id='preset4',
                enhancement=EnhancementMode.SMART_PATCH,
                proofread=ProofreadMode.FULL,
                translate=TranslateMode.FULL,
                target_language='en'
            ),
            'preset5': cls(
                preset_id='preset5',
                enhancement=EnhancementMode.SMART_PATCH,
                proofread=ProofreadMode.FULL,
                translate=TranslateMode.PARTIAL
            ),
        }
        return presets.get(preset_id, cls())


# ========== 方案矩阵描述 ==========

SOLUTION_MATRIX = {
    # ========== 基础层 ==========
    "A1": {
        "name": "SenseVoice Only",
        "flow": ["sensevoice"],
        "scenario": "实时预览、即时日志",
        "note": "极速。原汁原味，依靠前端标红提示错误"
    },
    "B1": {
        "name": "SenseVoice + Whisper Partial",
        "flow": ["sensevoice", "whisper_partial", "pseudo_align"],
        "scenario": "嘈杂、多BGM环境",
        "note": "默认推荐。仅对低置信度片段进行Whisper重听，伪对齐"
    },
    "C1": {
        "name": "SenseVoice + Whisper Full",
        "flow": ["sensevoice_as_vad", "whisper_full"],
        "scenario": "对比测试/极低质量音频",
        "note": "重制。用SV做VAD，Whisper跑全文"
    },

    # ========== 语义层（可叠加） ==========
    "P1": {
        "name": "LLM Partial Proof",
        "flow": ["llm_sparse_proof"],
        "scenario": "个人笔记、日常Vlog",
        "note": "性价比之王。按需稀疏校对，节省90%+Token"
    },
    "P2": {
        "name": "LLM Full Proof",
        "flow": ["llm_full_proof"],
        "scenario": "正式出版、文稿整理",
        "note": "高质量。全量滑动窗口，润色口语、修正逻辑"
    },

    # ========== 翻译层（必须在校对之后） ==========
    "T1": {
        "name": "LLM Full Trans",
        "flow": ["llm_full_translate"],
        "scenario": "跨语言内容",
        "note": "传统的全量翻译"
    },
    "T2": {
        "name": "LLM Partial Trans",
        "flow": ["llm_partial_translate"],
        "scenario": "教学重点标注",
        "note": "仅翻译用户指定的重点段落"
    }
}
```

---

## 七、字幕流式输出系统

### 7.1 流式输出原则

**文件**: `backend/app/services/streaming_subtitle.py`

```
┌─────────────────────────────────────────────────────────────────┐
│                    字幕流式输出时序                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  SenseVoice 转录阶段：                                          │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │
│  │ VAD段1  │→│分句1,2,3│→│ SSE推送 │   立即显示               │
│  └─────────┘  └─────────┘  └─────────┘                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │
│  │ VAD段2  │→│分句4,5  │→│ SSE推送 │   追加显示               │
│  └─────────┘  └─────────┘  └─────────┘                         │
│                                                                 │
│  Whisper 复核阶段：                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │
│  │ 句子3   │→│ Whisper │→│ SSE覆盖 │   覆盖句子3              │
│  │(低置信) │  │  复核   │  │ 句子3  │                          │
│  └─────────┘  └─────────┘  └─────────┘                         │
│                                                                 │
│  LLM 校对阶段：                                                 │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │
│  │ 句子5   │→│  LLM    │→│ SSE覆盖 │   覆盖句子5              │
│  │(需校对) │  │  校对   │  │ 句子5  │                          │
│  └─────────┘  └─────────┘  └─────────┘                         │
│                                                                 │
│  LLM 翻译阶段：                                                 │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                         │
│  │ 句子1   │→│  LLM    │→│ SSE追加 │   追加翻译字段            │
│  │         │  │  翻译   │  │translation│                       │
│  └─────────┘  └─────────┘  └─────────┘                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```python
"""
字幕流式输出管理器

核心职责：
1. SenseVoice 每完成一个 VAD 段的转录和分句，立即推送到前端
2. Whisper 每完成一句复核，立即覆盖前端对应句子
3. LLM 每完成一句校对/翻译，立即更新前端对应句子
"""
from typing import List, Dict, Optional
from dataclasses import dataclass
import logging

logger = logging.getLogger(__name__)


@dataclass
class SubtitleUpdateEvent:
    """字幕更新事件"""
    action: str          # "append" | "update" | "batch_update"
    sentence_index: int  # 句子索引（-1 表示批量）
    sentence_data: dict  # 句子数据
    source: str          # "sensevoice" | "whisper" | "llm_proof" | "llm_trans"


class StreamingSubtitleManager:
    """流式字幕管理器"""

    def __init__(self, job_id: str, sse_manager):
        self.job_id = job_id
        self.sse_manager = sse_manager
        self.channel_id = f"job:{job_id}"

        # 当前字幕列表（内存缓存）
        self.sentences: List[dict] = []

        logger.info(f"流式字幕管理器初始化: {job_id}")

    def on_sensevoice_vad_complete(
        self,
        vad_index: int,
        sentences: List[dict]
    ):
        """
        SenseVoice 完成一个 VAD 段的转录

        Args:
            vad_index: VAD 段索引
            sentences: 该段内的所有句子
        """
        start_index = len(self.sentences)

        for i, sent in enumerate(sentences):
            sent['index'] = start_index + i
            sent['source'] = 'sensevoice'
            self.sentences.append(sent)

            # 立即推送
            self.sse_manager.broadcast_sync(
                self.channel_id,
                "subtitle.sv_sentence",
                {
                    "action": "append",
                    "sentence_index": sent['index'],
                    "sentence": sent,
                    "vad_index": vad_index
                }
            )

        logger.debug(f"SenseVoice VAD{vad_index} 完成: {len(sentences)} 句")

    def on_whisper_patch_complete(
        self,
        sentence_index: int,
        new_text: str,
        new_confidence: float = 1.0
    ):
        """
        Whisper 完成一句复核

        Args:
            sentence_index: 句子索引
            new_text: 新文本
            new_confidence: 新置信度
        """
        if sentence_index >= len(self.sentences):
            logger.warning(f"Whisper 复核索引越界: {sentence_index}")
            return

        # 更新内存
        sent = self.sentences[sentence_index]
        sent['original_text'] = sent.get('text', '')
        sent['text'] = new_text
        sent['confidence'] = new_confidence
        sent['source'] = 'whisper_patch'
        sent['is_modified'] = True

        # 立即推送覆盖
        self.sse_manager.broadcast_sync(
            self.channel_id,
            "subtitle.whisper_patch",
            {
                "action": "update",
                "sentence_index": sentence_index,
                "sentence": sent
            }
        )

        logger.debug(f"Whisper 复核 句子{sentence_index}: '{sent['original_text']}' -> '{new_text}'")

    def on_llm_proof_complete(
        self,
        sentence_index: int,
        proofed_text: str,
        perplexity: float = None
    ):
        """
        LLM 完成一句校对

        Args:
            sentence_index: 句子索引
            proofed_text: 校对后文本
            perplexity: 困惑度
        """
        if sentence_index >= len(self.sentences):
            logger.warning(f"LLM 校对索引越界: {sentence_index}")
            return

        # 更新内存
        sent = self.sentences[sentence_index]
        if sent['text'] != proofed_text:
            sent['original_text'] = sent.get('original_text') or sent['text']
            sent['text'] = proofed_text
            sent['is_modified'] = True
        sent['source'] = 'llm_correction'
        sent['perplexity'] = perplexity

        # 计算警告类型
        sent['warning_type'] = self._compute_warning_type(sent)

        # 立即推送覆盖
        self.sse_manager.broadcast_sync(
            self.channel_id,
            "subtitle.llm_proof",
            {
                "action": "update",
                "sentence_index": sentence_index,
                "sentence": sent
            }
        )

        logger.debug(f"LLM 校对 句子{sentence_index}, perplexity={perplexity}")

    def on_llm_trans_complete(
        self,
        sentence_index: int,
        translation: str,
        translation_confidence: float = None
    ):
        """
        LLM 完成一句翻译

        Args:
            sentence_index: 句子索引
            translation: 翻译结果
            translation_confidence: 翻译置信度
        """
        if sentence_index >= len(self.sentences):
            logger.warning(f"LLM 翻译索引越界: {sentence_index}")
            return

        # 更新内存
        sent = self.sentences[sentence_index]
        sent['translation'] = translation
        sent['translation_confidence'] = translation_confidence

        # 立即推送（追加翻译字段）
        self.sse_manager.broadcast_sync(
            self.channel_id,
            "subtitle.llm_trans",
            {
                "action": "update",
                "sentence_index": sentence_index,
                "sentence": sent
            }
        )

        logger.debug(f"LLM 翻译 句子{sentence_index}: '{translation}'")

    def _compute_warning_type(self, sent: dict) -> str:
        """计算警告类型"""
        confidence = sent.get('confidence', 1.0)
        perplexity = sent.get('perplexity')

        has_low_confidence = confidence < 0.6
        has_high_perplexity = perplexity is not None and perplexity > 50.0

        if has_low_confidence and has_high_perplexity:
            return 'both'
        elif has_low_confidence:
            return 'low_transcription'
        elif has_high_perplexity:
            return 'high_perplexity'
        return 'none'

    def get_all_sentences(self) -> List[dict]:
        """获取所有句子"""
        return self.sentences


# ========== 单例工厂 ==========

_streaming_managers: Dict[str, StreamingSubtitleManager] = {}


def get_streaming_subtitle_manager(
    job_id: str,
    sse_manager = None
) -> StreamingSubtitleManager:
    """获取或创建流式字幕管理器"""
    global _streaming_managers

    if job_id not in _streaming_managers:
        if sse_manager is None:
            from .sse_service import get_sse_manager
            sse_manager = get_sse_manager()
        _streaming_managers[job_id] = StreamingSubtitleManager(job_id, sse_manager)

    return _streaming_managers[job_id]


def remove_streaming_subtitle_manager(job_id: str):
    """移除流式字幕管理器"""
    global _streaming_managers
    if job_id in _streaming_managers:
        del _streaming_managers[job_id]
```

---

## 八、置信度与困惑度阈值体系

### 8.1 阈值定义与合理性分析

**文件**: `backend/app/core/thresholds.py`

```python
"""
置信度与困惑度阈值配置

基于实际测试和业界经验设定阈值：
- 转录置信度：基于 Whisper avg_logprob 和 no_speech_prob
- 校对困惑度：基于 LLM 输出的 perplexity
"""
from dataclasses import dataclass
from enum import Enum


class ConfidenceLevel(Enum):
    """置信度等级"""
    HIGH = "high"           # 高置信度，无需处理
    MEDIUM = "medium"       # 中等置信度，可选处理
    LOW = "low"             # 低置信度，需要处理
    CRITICAL = "critical"   # 极低置信度，必须处理


@dataclass
class ThresholdConfig:
    """阈值配置"""

    # ========== 转录置信度阈值 ==========

    # SenseVoice 置信度（0-1 范围，字级/句级）
    sv_confidence_high: float = 0.85      # >= 高置信度，绿色/无标记
    sv_confidence_medium: float = 0.6     # >= 中等置信度，黄色警告
    sv_confidence_low: float = 0.4        # >= 低置信度，橙色警告
    # < 0.4 为极低置信度，红色警告

    # Whisper avg_logprob 阈值（负值，越接近0越好）
    # 参考：Whisper 官方建议 -0.8 以下需要关注
    whisper_logprob_good: float = -0.5    # >= 高质量
    whisper_logprob_ok: float = -0.8      # >= 可接受（当前系统使用）
    whisper_logprob_bad: float = -1.0     # >= 较差
    # < -1.0 为极差

    # Whisper no_speech_prob 阈值（0-1，越低越好）
    # 参考：官方建议 0.6 以上可能是静音
    whisper_no_speech_low: float = 0.3    # <= 可信
    whisper_no_speech_medium: float = 0.6 # <= 可接受（当前系统使用）
    whisper_no_speech_high: float = 0.8   # <= 可疑
    # > 0.8 极可能是静音/噪音

    # ========== 校对困惑度阈值 ==========

    # LLM 困惑度（perplexity，越低越好）
    # 参考：GPT-2 在常规文本上约 20-40
    llm_perplexity_excellent: float = 20.0   # <= 优秀
    llm_perplexity_good: float = 35.0        # <= 良好
    llm_perplexity_acceptable: float = 50.0  # <= 可接受
    llm_perplexity_poor: float = 80.0        # <= 较差
    # > 80 需要人工检查

    # ========== 触发阈值 ==========

    # Whisper 复核触发阈值
    whisper_patch_trigger_confidence: float = 0.6  # SV 置信度低于此值触发
    whisper_patch_trigger_logprob: float = -0.8    # 或 logprob 低于此值

    # LLM 校对触发阈值（按需校对模式）
    llm_proof_trigger_confidence: float = 0.7      # 置信度低于此值需要校对
    llm_proof_trigger_modified: bool = True        # 被 Whisper 修改过的需要校对

    # ========== 警告高亮阈值 ==========

    # 字级警告阈值
    word_warning_confidence: float = 0.5           # 字级置信度低于此值显示黄色
    word_critical_confidence: float = 0.3          # 字级置信度低于此值显示红色

    # 句级警告阈值
    sentence_warning_confidence: float = 0.6       # 句级置信度低于此值显示警告
    sentence_warning_perplexity: float = 50.0      # 困惑度高于此值显示警告


# 默认配置实例
DEFAULT_THRESHOLDS = ThresholdConfig()


def get_confidence_level(confidence: float, config: ThresholdConfig = None) -> ConfidenceLevel:
    """
    根据置信度获取等级

    Args:
        confidence: 置信度值 (0-1)
        config: 阈值配置

    Returns:
        ConfidenceLevel: 置信度等级
    """
    if config is None:
        config = DEFAULT_THRESHOLDS

    if confidence >= config.sv_confidence_high:
        return ConfidenceLevel.HIGH
    elif confidence >= config.sv_confidence_medium:
        return ConfidenceLevel.MEDIUM
    elif confidence >= config.sv_confidence_low:
        return ConfidenceLevel.LOW
    else:
        return ConfidenceLevel.CRITICAL


def needs_whisper_patch(
    confidence: float,
    avg_logprob: float = None,
    no_speech_prob: float = None,
    config: ThresholdConfig = None
) -> bool:
    """
    判断是否需要 Whisper 复核

    Args:
        confidence: SenseVoice 置信度
        avg_logprob: Whisper 平均 logprob（如果已有）
        no_speech_prob: Whisper 静音概率（如果已有）
        config: 阈值配置

    Returns:
        bool: 是否需要复核
    """
    if config is None:
        config = DEFAULT_THRESHOLDS

    # 条件1：SenseVoice 置信度低
    if confidence < config.whisper_patch_trigger_confidence:
        return True

    # 条件2：已有 Whisper 结果但质量差
    if avg_logprob is not None and avg_logprob < config.whisper_patch_trigger_logprob:
        return True

    if no_speech_prob is not None and no_speech_prob > config.whisper_no_speech_medium:
        return True

    return False


def needs_llm_proof(
    confidence: float,
    is_modified: bool = False,
    perplexity: float = None,
    mode: str = 'sparse',
    config: ThresholdConfig = None
) -> bool:
    """
    判断是否需要 LLM 校对

    Args:
        confidence: 转录置信度
        is_modified: 是否被 Whisper 修改过
        perplexity: 当前困惑度（如果已有）
        mode: 校对模式 ('sparse' | 'full')
        config: 阈值配置

    Returns:
        bool: 是否需要校对
    """
    if config is None:
        config = DEFAULT_THRESHOLDS

    # 全文模式：总是校对
    if mode == 'full':
        return True

    # 按需模式
    if mode == 'sparse':
        # 条件1：置信度低
        if confidence < config.llm_proof_trigger_confidence:
            return True

        # 条件2：被 Whisper 修改过
        if is_modified and config.llm_proof_trigger_modified:
            return True

        # 条件3：已知困惑度高
        if perplexity is not None and perplexity > config.llm_perplexity_acceptable:
            return True

    return False
```

### 8.2 阈值合理性说明

| 阈值类型 | 当前值 | 依据 | 建议范围 |
|----------|--------|------|----------|
| **SV 置信度高** | 0.85 | SenseVoice 官方测试表现 | 0.80-0.90 |
| **SV 置信度触发复核** | 0.6 | 实测低于此值错误率显著上升 | 0.55-0.70 |
| **Whisper logprob** | -0.8 | OpenAI 官方建议 | -0.7 ~ -1.0 |
| **Whisper no_speech** | 0.6 | OpenAI 官方建议 | 0.5-0.7 |
| **LLM 困惑度可接受** | 50.0 | GPT-2 基准 + 经验调整 | 40-60 |
| **字级警告** | 0.5 | 用户可接受的错误显示密度 | 0.4-0.6 |

---

## 九、警告高亮系统

### 9.1 字级警告高亮

**文件**: `frontend/src/components/ConfidenceHighlight.vue`

```vue
<template>
  <span
    :class="wordClass"
    :title="tooltipText"
    @click="$emit('word-click', word)"
  >
    {{ word.word }}
  </span>
</template>

<script setup>
import { computed } from 'vue';

const props = defineProps({
  word: {
    type: Object,
    required: true
  },
  showTooltip: {
    type: Boolean,
    default: true
  }
});

defineEmits(['word-click']);

// 阈值配置
const THRESHOLDS = {
  word: {
    high: 0.85,
    medium: 0.6,
    low: 0.4,
    critical: 0.3
  },
  perplexity: {
    good: 35,
    acceptable: 50,
    poor: 80
  }
};

const wordClass = computed(() => {
  const confidence = props.word.confidence || 1.0;
  const perplexity = props.word.perplexity;
  const warningType = props.word.warning_type || 'none';

  const classes = ['word-span'];

  // 转录置信度警告
  if (confidence >= THRESHOLDS.word.high) {
    classes.push('confidence-high');
  } else if (confidence >= THRESHOLDS.word.medium) {
    classes.push('confidence-medium');
  } else if (confidence >= THRESHOLDS.word.low) {
    classes.push('confidence-low');
  } else {
    classes.push('confidence-critical');
  }

  // 校对困惑度警告
  if (perplexity !== null && perplexity !== undefined) {
    if (perplexity > THRESHOLDS.perplexity.poor) {
      classes.push('perplexity-high');
    } else if (perplexity > THRESHOLDS.perplexity.acceptable) {
      classes.push('perplexity-medium');
    }
  }

  // 特殊标记
  if (props.word.is_pseudo) {
    classes.push('is-pseudo-align');
  }

  return classes;
});

const tooltipText = computed(() => {
  if (!props.showTooltip) return '';

  const parts = [];
  const confidence = props.word.confidence;
  const perplexity = props.word.perplexity;

  if (confidence < THRESHOLDS.word.high) {
    parts.push(`转录置信度: ${(confidence * 100).toFixed(1)}%`);
  }

  if (perplexity !== null && perplexity !== undefined) {
    if (perplexity > THRESHOLDS.perplexity.acceptable) {
      parts.push(`校对困惑度: ${perplexity.toFixed(1)}`);
    }
  }

  if (props.word.is_pseudo) {
    parts.push('(伪对齐)');
  }

  return parts.join(' | ');
});
</script>

<style scoped>
.word-span {
  display: inline;
  cursor: pointer;
  transition: background-color 0.2s;
}

/* ========== 转录置信度样式 ========== */
.confidence-high {
  /* 高置信度：无特殊样式 */
}

.confidence-medium {
  /* 中等置信度：淡黄色背景 */
  background-color: rgba(255, 235, 59, 0.3);
  border-bottom: 1px dashed #ffc107;
}

.confidence-low {
  /* 低置信度：橙色背景 */
  background-color: rgba(255, 152, 0, 0.4);
  border-bottom: 2px solid #ff9800;
}

.confidence-critical {
  /* 极低置信度：红色背景 */
  background-color: rgba(244, 67, 54, 0.4);
  border-bottom: 2px solid #f44336;
  color: #d32f2f;
}

/* ========== 校对困惑度样式（叠加） ========== */
.perplexity-medium {
  /* 中等困惑度：虚线下划线 */
  text-decoration: underline wavy #9c27b0;
}

.perplexity-high {
  /* 高困惑度：粗虚线下划线 */
  text-decoration: underline wavy #e91e63;
  text-decoration-thickness: 2px;
}

/* ========== 伪对齐标记 ========== */
.is-pseudo-align {
  font-style: italic;
  opacity: 0.9;
}

/* ========== 悬停效果 ========== */
.word-span:hover {
  background-color: rgba(33, 150, 243, 0.2);
}
</style>
```

### 9.2 句级警告高亮

**文件**: `frontend/src/components/SentenceWarning.vue`

```vue
<template>
  <div :class="sentenceClass">
    <!-- 警告图标 -->
    <span v-if="hasWarning" class="warning-icon" :title="warningTooltip">
      <el-icon v-if="warningType === 'low_transcription'"><Warning /></el-icon>
      <el-icon v-else-if="warningType === 'high_perplexity'"><QuestionFilled /></el-icon>
      <el-icon v-else-if="warningType === 'both'"><CircleCloseFilled /></el-icon>
    </span>

    <!-- 句子内容 -->
    <span class="sentence-content">
      <slot></slot>
    </span>

    <!-- 置信度/困惑度指示器 -->
    <span v-if="showIndicator" class="indicators">
      <span v-if="confidence < 0.85" class="confidence-badge" :class="confidenceLevel">
        {{ (confidence * 100).toFixed(0) }}%
      </span>
      <span v-if="perplexity && perplexity > 35" class="perplexity-badge" :class="perplexityLevel">
        P:{{ perplexity.toFixed(0) }}
      </span>
    </span>
  </div>
</template>

<script setup>
import { computed } from 'vue';
import { Warning, QuestionFilled, CircleCloseFilled } from '@element-plus/icons-vue';

const props = defineProps({
  sentence: {
    type: Object,
    required: true
  },
  showIndicator: {
    type: Boolean,
    default: true
  }
});

const confidence = computed(() => props.sentence.confidence || 1.0);
const perplexity = computed(() => props.sentence.perplexity);
const warningType = computed(() => props.sentence.warning_type || 'none');

const hasWarning = computed(() => warningType.value !== 'none');

const confidenceLevel = computed(() => {
  const c = confidence.value;
  if (c >= 0.85) return 'level-high';
  if (c >= 0.6) return 'level-medium';
  if (c >= 0.4) return 'level-low';
  return 'level-critical';
});

const perplexityLevel = computed(() => {
  const p = perplexity.value;
  if (!p) return '';
  if (p <= 35) return 'level-good';
  if (p <= 50) return 'level-acceptable';
  if (p <= 80) return 'level-poor';
  return 'level-critical';
});

const sentenceClass = computed(() => {
  const classes = ['sentence-row'];

  if (warningType.value === 'low_transcription') {
    classes.push('warning-transcription');
  } else if (warningType.value === 'high_perplexity') {
    classes.push('warning-perplexity');
  } else if (warningType.value === 'both') {
    classes.push('warning-both');
  }

  if (props.sentence.is_modified) {
    classes.push('is-modified');
  }

  return classes;
});

const warningTooltip = computed(() => {
  switch (warningType.value) {
    case 'low_transcription':
      return `转录置信度低 (${(confidence.value * 100).toFixed(1)}%)，建议检查`;
    case 'high_perplexity':
      return `校对困惑度高 (${perplexity.value?.toFixed(1)})，可能存在语义问题`;
    case 'both':
      return `转录置信度低且校对困惑度高，需要重点关注`;
    default:
      return '';
  }
});
</script>

<style scoped>
.sentence-row {
  display: flex;
  align-items: flex-start;
  padding: 8px 12px;
  border-radius: 4px;
  margin: 4px 0;
  transition: background-color 0.2s;
}

/* ========== 警告类型样式 ========== */
.warning-transcription {
  background-color: rgba(255, 152, 0, 0.1);
  border-left: 3px solid #ff9800;
}

.warning-perplexity {
  background-color: rgba(156, 39, 176, 0.1);
  border-left: 3px solid #9c27b0;
}

.warning-both {
  background-color: rgba(244, 67, 54, 0.1);
  border-left: 3px solid #f44336;
}

.is-modified {
  background-color: rgba(33, 150, 243, 0.05);
}

/* ========== 警告图标 ========== */
.warning-icon {
  margin-right: 8px;
  flex-shrink: 0;
}

.warning-transcription .warning-icon {
  color: #ff9800;
}

.warning-perplexity .warning-icon {
  color: #9c27b0;
}

.warning-both .warning-icon {
  color: #f44336;
}

/* ========== 指示器 ========== */
.indicators {
  margin-left: auto;
  display: flex;
  gap: 4px;
}

.confidence-badge,
.perplexity-badge {
  font-size: 11px;
  padding: 2px 6px;
  border-radius: 10px;
}

.level-high { background-color: #e8f5e9; color: #4caf50; }
.level-medium { background-color: #fff3e0; color: #ff9800; }
.level-low { background-color: #ffebee; color: #f44336; }
.level-critical { background-color: #f44336; color: white; }
.level-good { background-color: #e8f5e9; color: #4caf50; }
.level-acceptable { background-color: #fff3e0; color: #ff9800; }
.level-poor { background-color: #fce4ec; color: #e91e63; }
</style>
```

### 9.3 警告类型区分说明

| 警告类型 | 触发条件 | 视觉表现 | 含义 |
|----------|----------|----------|------|
| **转录置信度低** | `confidence < 0.6` | 橙色边框 + ⚠️ 图标 | ASR 模型对此段识别不确定 |
| **校对困惑度高** | `perplexity > 50` | 紫色边框 + ❓ 图标 | LLM 认为文本可能存在语义问题 |
| **双重警告** | 两者都满足 | 红色边框 + ⊗ 图标 | 需要人工重点检查 |

---

## 十、主处理流程重构

### 10.1 优化后的 Pipeline（v2.1 VAD优先版）

```
┌─────────────────────────────────────────────────────────────────┐
│                    时空解耦处理流程（v2.1）                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 音频提取 [extract]                                          │
│     ↓  → SSE: progress.extract                                  │
│  2. VAD 物理切分 [vad]（15-30s Chunks）                         │
│     ↓  → SSE: progress.vad                                      │
│  3. 频谱分诊 [spectrum]（Chunk级别）                            │
│     │  - AudioSpectrumClassifier 提取频谱特征                   │
│     │  - 判断：CLEAN / MUSIC / NOISE / MIXED                    │
│     │  - 推荐模型：htdemucs / mdx_extra / None                  │
│     ↓  → SSE: progress.spectrum                                 │
│  4. 人声分离 [demucs]（仅对需要分离的Chunk）                    │
│     ↓  → SSE: progress.demucs                                   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 5. SenseVoice 转录 [sensevoice]（时间领主）                 ││
│  │    - 每完成一个 VAD 段立即推送                              ││
│  │    → SSE: subtitle.sv_segment（流式）                       ││
│  │    → SSE: progress.sensevoice（按段更新）                   ││
│  └─────────────────────────────────────────────────────────────┘│
│     ↓                                                           │
│  6. 分句算法                                                    │
│     ↓  → SSE: subtitle.sv_sentence（每句推送）                  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 7. Whisper 复核 [whisper]（听觉补丁，可选）                 ││
│  │    - 仅处理低置信度句子                                     ││
│  │    - 每完成一句立即覆盖                                     ││
│  │    → SSE: subtitle.whisper_patch（覆盖推送）                ││
│  │    → SSE: progress.whisper（按完成/待处理段数更新）         ││
│  └─────────────────────────────────────────────────────────────┘│
│     ↓                                                           │
│  ═══════════════════════════════════════════════════════════════│
│  │               语义增强层（根据预设启用）                    ││
│  ═══════════════════════════════════════════════════════════════│
│     ↓                                                           │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 8. LLM 校对 [llm_proof]（可选）                             ││
│  │    - 每完成一句立即覆盖                                     ││
│  │    → SSE: subtitle.llm_proof（覆盖推送）                    ││
│  │    → SSE: progress.llm_proof（按完成/待处理段数更新）       ││
│  └─────────────────────────────────────────────────────────────┘│
│     ↓                                                           │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 9. LLM 翻译 [llm_trans]（可选）                             ││
│  │    - 每完成一句追加翻译字段                                 ││
│  │    → SSE: subtitle.llm_trans（追加推送）                    ││
│  │    → SSE: progress.llm_trans（按完成/待处理段数更新）       ││
│  └─────────────────────────────────────────────────────────────┘│
│     ↓                                                           │
│  10. SRT 生成 [srt]                                             │
│     ↓  → SSE: progress.srt                                      │
│  11. 完成                                                       │
│      → SSE: signal.job_complete                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 核心流程代码

```python
async def _process_video_sensevoice_v2(self, job: JobState):
    """
    SenseVoice 主处理流程（VAD优先 + 时空解耦版 v2.1）
    """
    from .progress_tracker import get_progress_tracker, ProcessPhase
    from .streaming_subtitle import get_streaming_subtitle_manager
    from .solution_matrix import SolutionConfig
    from .audio_spectrum_classifier import get_spectrum_classifier

    # 获取配置
    solution_config = SolutionConfig.from_preset(job.settings.preset_id)
    progress = get_progress_tracker(job.job_id, solution_config.preset_id)
    subtitle_manager = get_streaming_subtitle_manager(job.job_id, self.sse_manager)

    try:
        # === 阶段 1: 音频提取 ===
        progress.start_phase(ProcessPhase.EXTRACT, message="提取音频中...")
        audio_path, audio_array = await self._extract_audio_with_array(job)
        progress.complete_phase(ProcessPhase.EXTRACT)

        # === 阶段 2: VAD 分段（优先执行） ===
        progress.start_phase(ProcessPhase.VAD, message="语音活动检测...")
        vad_segments = await self._vad_physical_split(audio_path, audio_array, job)
        progress.complete_phase(ProcessPhase.VAD)

        # === 阶段 3: 频谱分诊（Chunk级别） ===
        progress.start_phase(ProcessPhase.SPECTRUM, message="频谱分诊...")
        spectrum_classifier = get_spectrum_classifier()
        diagnoses = spectrum_classifier.diagnose_chunks(
            [(audio_array[int(s['start']*16000):int(s['end']*16000)], s['start'], s['end'])
             for s in vad_segments]
        )
        progress.complete_phase(ProcessPhase.SPECTRUM)

        # === 阶段 4: 人声分离（仅对需要分离的Chunk） ===
        chunks_to_separate = [(i, vad_segments[i], diagnoses[i])
                              for i in range(len(diagnoses))
                              if diagnoses[i].need_separation]

        if chunks_to_separate:
            progress.start_phase(
                ProcessPhase.DEMUCS,
                total_items=len(chunks_to_separate),
                message=f"分离人声 (0/{len(chunks_to_separate)} chunks)..."
            )

            for sep_idx, (chunk_idx, vad_seg, diag) in enumerate(chunks_to_separate):
                # 按推荐模型分离
                separated_audio = await self._separate_chunk(
                    audio_array, vad_seg, diag.recommended_model, job
                )
                # 替换原始音频段
                vad_segments[chunk_idx]['processed_audio'] = separated_audio

                progress.update_phase(ProcessPhase.DEMUCS, completed=sep_idx + 1)

            progress.complete_phase(ProcessPhase.DEMUCS)

        # === 阶段 5-6: SenseVoice 转录（流式） ===
        progress.start_phase(
            ProcessPhase.SENSEVOICE,
            total_items=len(vad_segments),
            message="SenseVoice 转录中..."
        )

        all_sentences = []
        for vad_idx, vad_seg in enumerate(vad_segments):
            # 检查取消
            if job.canceled:
                raise RuntimeError('任务已取消')

            # 转录单个 VAD 段
            sentences = await self._transcribe_vad_segment(
                vad_seg, audio_array, job
            )

            # 流式推送（每个 VAD 段完成后）
            subtitle_manager.on_sensevoice_vad_complete(vad_idx, sentences)
            all_sentences.extend(sentences)

            # 更新进度
            progress.update_phase(ProcessPhase.SENSEVOICE, completed=vad_idx + 1)
            self._push_progress_update(job, progress)

        progress.complete_phase(ProcessPhase.SENSEVOICE)

        # === 阶段 7: Whisper 复核（可选） ===
        if solution_config.enhancement != EnhancementMode.OFF:
            # 筛选需要复核的句子
            low_confidence_sentences = [
                (i, s) for i, s in enumerate(all_sentences)
                if s['confidence'] < solution_config.confidence_threshold
            ]

            if low_confidence_sentences:
                progress.start_phase(
                    ProcessPhase.WHISPER_PATCH,
                    total_items=len(low_confidence_sentences),
                    message=f"Whisper 复核中 (0/{len(low_confidence_sentences)})..."
                )

                for patch_idx, (sent_idx, sentence) in enumerate(low_confidence_sentences):
                    if job.canceled:
                        raise RuntimeError('任务已取消')

                    # Whisper 复核
                    new_text = await self._whisper_patch_single(
                        sentence, audio_array, job
                    )

                    # 流式覆盖
                    subtitle_manager.on_whisper_patch_complete(
                        sent_idx, new_text
                    )

                    # 更新进度
                    progress.update_phase(
                        ProcessPhase.WHISPER_PATCH,
                        completed=patch_idx + 1,
                        message=f"Whisper 复核中 ({patch_idx + 1}/{len(low_confidence_sentences)})..."
                    )
                    self._push_progress_update(job, progress)

                progress.complete_phase(ProcessPhase.WHISPER_PATCH)

        # === 阶段 8: LLM 校对（可选） ===
        if solution_config.proofread != ProofreadMode.OFF:
            sentences_to_proof = self._get_sentences_for_proof(
                all_sentences, solution_config
            )

            if sentences_to_proof:
                progress.start_phase(
                    ProcessPhase.LLM_PROOF,
                    total_items=len(sentences_to_proof),
                    message=f"LLM 校对中 (0/{len(sentences_to_proof)})..."
                )

                for proof_idx, (sent_idx, sentence) in enumerate(sentences_to_proof):
                    proofed_text, perplexity = await self._llm_proof_single(
                        sentence, job
                    )

                    # 流式覆盖
                    subtitle_manager.on_llm_proof_complete(
                        sent_idx, proofed_text, perplexity
                    )

                    progress.update_phase(
                        ProcessPhase.LLM_PROOF,
                        completed=proof_idx + 1,
                        message=f"LLM 校对中 ({proof_idx + 1}/{len(sentences_to_proof)})..."
                    )
                    self._push_progress_update(job, progress)

                progress.complete_phase(ProcessPhase.LLM_PROOF)

        # === 阶段 9: LLM 翻译（可选） ===
        if solution_config.translate != TranslateMode.OFF:
            sentences_to_trans = self._get_sentences_for_translate(
                all_sentences, solution_config
            )

            if sentences_to_trans:
                progress.start_phase(
                    ProcessPhase.LLM_TRANS,
                    total_items=len(sentences_to_trans),
                    message=f"LLM 翻译中 (0/{len(sentences_to_trans)})..."
                )

                for trans_idx, (sent_idx, sentence) in enumerate(sentences_to_trans):
                    translation = await self._llm_translate_single(
                        sentence, solution_config.target_language, job
                    )

                    # 流式追加
                    subtitle_manager.on_llm_trans_complete(
                        sent_idx, translation
                    )

                    progress.update_phase(
                        ProcessPhase.LLM_TRANS,
                        completed=trans_idx + 1,
                        message=f"LLM 翻译中 ({trans_idx + 1}/{len(sentences_to_trans)})..."
                    )
                    self._push_progress_update(job, progress)

                progress.complete_phase(ProcessPhase.LLM_TRANS)

        # === 阶段 10: SRT 生成 ===
        progress.start_phase(ProcessPhase.SRT, message="生成字幕文件...")
        final_sentences = subtitle_manager.get_all_sentences()
        srt_path = await self._generate_srt_from_sentences(final_sentences, job)
        progress.complete_phase(ProcessPhase.SRT)

        # === 完成 ===
        job.status = 'completed'
        job.srt_path = str(srt_path)
        self._push_signal_event(job, "job_complete", "转录完成")

    except Exception as e:
        self.logger.error(f"处理失败: {e}", exc_info=True)
        job.status = 'failed'
        job.error = str(e)
        self._push_signal_event(job, "job_failed", str(e))
        raise
```

---

## 十一、与 LLM 校对/翻译层的对接接口

本文档定义的转录层输出以下数据结构，供 LLM 校对/翻译层使用：

### 11.1 输出数据格式

```python
@dataclass
class TranscriptionOutput:
    """转录层输出（供 LLM 层使用）"""
    sentences: List[SentenceSegment]  # 句级字幕列表
    metadata: dict                     # 元数据

    def to_llm_input(self) -> List[dict]:
        """
        转换为 LLM 输入格式

        Returns:
            适合 LLM 处理的句子列表
        """
        return [
            {
                "index": i,
                "text": s.text,
                "start": s.start,
                "end": s.end,
                "confidence": s.confidence,
                "source": s.source.value,
                "warning_type": s.warning_type.value,
                "needs_review": s.confidence < 0.7 or s.is_modified
            }
            for i, s in enumerate(self.sentences)
        ]
```

### 11.2 接口约定

| 接口 | 描述 | 输入 | 输出 |
|------|------|------|------|
| `get_transcription_result()` | 获取转录结果 | `job_id` | `TranscriptionOutput` |
| `mark_for_review(indexes)` | 标记需要 LLM 审核的句子 | `List[int]` | `void` |
| `apply_llm_correction(index, text, perplexity)` | 应用 LLM 校对结果 | `int, str, float` | `SentenceSegment` |
| `apply_llm_translation(index, translation)` | 应用 LLM 翻译结果 | `int, str` | `SentenceSegment` |

详细的 LLM 集成方案见：[07_校对翻译层_LLM集成方案.md](./07_校对翻译层_LLM集成方案.md)

---

## 十二、前端虚拟滚动

### 12.1 为什么需要虚拟滚动

字级置信度数据量虽小但 DOM 节点极多：
- 10分钟视频 ≈ 3000 字
- 每个字一个 `<span>` 节点
- 直接渲染会导致页面卡顿

### 12.2 实现方案

```vue
<template>
  <div class="virtual-scroll-container" ref="container" @scroll="onScroll">
    <div class="virtual-scroll-phantom" :style="{ height: totalHeight + 'px' }"></div>
    <div class="virtual-scroll-content" :style="{ transform: `translateY(${offsetY}px)` }">
      <SentenceWarning
        v-for="item in visibleItems"
        :key="item.index"
        :sentence="item"
      >
        <ConfidenceHighlight
          v-for="word in item.words"
          :key="word.start"
          :word="word"
        />
      </SentenceWarning>
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue';
import ConfidenceHighlight from './ConfidenceHighlight.vue';
import SentenceWarning from './SentenceWarning.vue';

const props = defineProps({
  sentences: { type: Array, default: () => [] }
});

const container = ref(null);
const offsetY = ref(0);
const scrollTop = ref(0);

const itemHeight = 60;  // 每个句子的估计高度
const containerHeight = ref(600);

const totalHeight = computed(() => props.sentences.length * itemHeight);

const visibleCount = computed(() => 
  Math.ceil(containerHeight.value / itemHeight) + 5  // 多渲染几个
);

const startIndex = computed(() => 
  Math.max(0, Math.floor(scrollTop.value / itemHeight) - 2)
);

const visibleItems = computed(() => 
  props.sentences.slice(startIndex.value, startIndex.value + visibleCount.value)
);

function onScroll() {
  scrollTop.value = container.value.scrollTop;
  offsetY.value = startIndex.value * itemHeight;
}

onMounted(() => {
  if (container.value) {
    containerHeight.value = container.value.clientHeight;
  }
});
</script>
```

---

## 十三、验收标准

### 13.1 功能验收

- [ ] 前端预设选择器正常工作（6 个预设 + 高级自定义）
- [ ] SenseVoice 时间戳在整个流程中保持不变
- [ ] Whisper 复核后使用伪对齐算法生成字级时间戳
- [ ] 伪对齐算法正确生成字级时间戳
- [ ] `source` 字段正确追踪文本来源
- [ ] 智能进度系统根据预设动态调整权重
- [ ] 字幕流式输出：SV/Whisper/LLM 各阶段即时推送
- [ ] 警告高亮系统区分转录置信度低和校对困惑度高

### 13.2 SSE 事件验收

- [ ] `progress.*` 事件正确推送各阶段进度
- [ ] `subtitle.sv_segment` 每个 VAD 段完成后推送
- [ ] `subtitle.whisper_patch` 每句复核完成后覆盖推送
- [ ] `subtitle.llm_proof` 每句校对完成后覆盖推送
- [ ] `subtitle.llm_trans` 每句翻译完成后追加推送
- [ ] `signal.*` 事件正确推送任务状态变化

### 13.3 阈值验收

- [ ] SV 置信度阈值 0.6 触发 Whisper 复核合理
- [ ] Whisper logprob 阈值 -0.8 判断转录质量合理
- [ ] LLM 困惑度阈值 50.0 判断校对质量合理
- [ ] 字级警告阈值 0.5 显示密度合理

### 13.4 性能验收

- [ ] 10分钟视频处理时间 < 2分钟（默认预设）
- [ ] 10分钟视频处理时间 < 5分钟（深度校对预设）
- [ ] 字级数据量大时前端不卡顿（虚拟滚动）
- [ ] 伪对齐计算耗时 < 1ms/句
- [ ] SSE 推送延迟 < 100ms

### 13.5 兼容性验收

- [ ] 向后兼容现有任务配置
- [ ] LLM 层接口预留完整
- [ ] 前端平滑升级（旧任务可正常查看）

---

## 十四、下一步

本文档完成转录层的时空解耦架构设计，包括：
1. **前端预设系统**：6 个预设 + 高级自定义模式
2. **伪对齐算法**：Whisper 复核后使用线性时间平摊策略
3. **智能进度系统**：基于预设动态调整权重
4. **流式字幕输出**：SV/Whisper/LLM 各阶段即时推送
5. **阈值体系设计**：转录置信度与校对困惑度分离
6. **警告高亮系统**：字级与句级双层视觉反馈

**相关文档**：
- 前置：[Phase 1-5 修订版文档]
- 后续：[07_LLM校对层详细设计]（待创建）
- 后续：[08_前端组件详细设计]（待创建）

**实施顺序建议**：
1. 后端先实现 `ThresholdConfig` 和 `WarningType` 数据模型
2. 修改 `TranscriptionService` 支持预设配置和智能进度
3. 实现 `StreamingSubtitleManager` 流式推送
4. 前端实现预设选择器和高级设置弹窗
5. 前端实现警告高亮组件（`ConfidenceHighlight.vue`、`SentenceWarning.vue`）
6. 集成测试各预设场景
- 后续：[07_校对翻译层_LLM集成方案.md](./07_校对翻译层_LLM集成方案.md)
