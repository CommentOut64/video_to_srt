阶段1：生命周期与重启状态修正
抽 JobLifecycleService（任务创建/暂停/恢复/取消/检查点/元信息）。
增加“重启状态纠偏”：重启后发现 processing/transcribing 但无运行上下文 → 统一标记为 paused（或 failed，需你确认）。
阶段2：流水线编排解耦
抽 PipelineOrchestrator：_run_pipeline_v2、_run_new_preprocessing、profile选择、上下文恢复。
阶段3：字幕输出与 SSE 解耦
抽 SubtitleOutputService + SSEPublisher，集中处理字幕写入、快照、进度/事件推送。
阶段4：参数与模型缓存解耦
抽 TranscribeParamBuilder（_build_whisper_transcribe_params）与 ModelCacheService（clear_model_cache）。
阶段5：文档同步与回归测试
同步 /llmdoc，跑最小集成测试。